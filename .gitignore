# ── PyInstaller 打包产物 ──
dist/
build/
# 顶层打包产物（冗余副本，正式产物在 dist/）
_internal/
ReShade Launcher Build.exe
ReShade Debug.exe

# ── Python 缓存/虚拟环境 ──
__pycache__/
*.pyc
.venv/

# ── 用户配置/运行数据（不入库） ──
custom_data/
launch_config/
MOD_loader/

# ── 日志 ──
*.log

# ── 系统文件 ──
Thumbs.db
.DS_Store
